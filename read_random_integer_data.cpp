#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>

using namespace std;

int main(){
    // freopen("input.txt","r",stdin);
    int n,i = 0;
    while(scanf("%d",&n) == 1){ // CTRL + X to terminate loop
        i++;
    }
    cout << i << endl;
    return 0;
}
